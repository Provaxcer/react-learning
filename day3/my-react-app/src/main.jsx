import React from "react";
import ReactDOM from "react-dom/client";
import TradeStatus from "./App";

const root = ReactDOM.createRoot(
  document.getElementById("root")
);

root.render(<TradeStatus />);